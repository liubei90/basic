from setuptools import setup, find_packages

setup(
    name='module-manager',
    version='0.0.1',
    description='包管理，api调用',
    author='lb',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.6'
)
